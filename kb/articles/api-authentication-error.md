# API Authentication Error

For API authentication errors (e.g., 401/403): verify API keys/scopes have not rotated, check expiry and clock skew, confirm you send the Authorization header/OAuth bearer token correctly, validate environment (sandbox vs prod), inspect recent permission changes and audit logs.

## Keywords

401, 403, api, token, oauth, bearer, authentication, authorization, key, webhook, integration, endpoint, rate limit
