# Set to "ollama" to let the chatbot call Ollama /api/chat (KB-grounded replies). Persona detection stays keyword-only either way.
LLM_PROVIDER=none
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
